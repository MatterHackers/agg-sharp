<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Company>MatterHackers Inc.</Company>
    <ReleaseVersion>2.20.12</ReleaseVersion>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="ObjParser\**" />
    <EmbeddedResource Remove="ObjParser\**" />
    <None Remove="ObjParser\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ManifoldNET" Version="1.0.7-alpha" />
    <!-- The pure-Rust boolean backend, selected at run time by BooleanProcessing.UseManifoldRust.
         Both engines ship: ManifoldNET stays the default until the Rust one has been exercised. -->
    <PackageReference Include="ManifoldRust" Version="0.2.0" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.9" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="MIConvexHull" Version="1.1.19.1019" />
  </ItemGroup>

  <ItemGroup>
    <!-- Same arrangement agg and RenderGl already use: lets the tests reach the boolean
         backends directly, so a rejected input can be observed as the exception it throws
         rather than only as the CsgBySlicing fallback that hides it. -->
    <InternalsVisibleTo Include="Agg.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\agg\Agg.csproj" />
    <ProjectReference Include="..\clipper_library\clipper_library.csproj" />
    <ProjectReference Include="..\DataConverters2D\DataConverters2D.csproj" />
    <ProjectReference Include="..\geometry3Sharp\geometry3Sharp.csproj" />
    <ProjectReference Include="..\QuadTree\QuadTree.csproj" />
    <ProjectReference Include="..\Triangle\Triangle.csproj" />
    <ProjectReference Include="..\VectorMath\VectorMath.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Update="StyleCop.Analyzers" Version="1.1.118" />
  </ItemGroup>

</Project>